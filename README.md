# Fake-Inventory-Data-Generator

Use this script to generate some fake json data.

To run it :
pip install fake-factory pytz

python fake_inventory_replenishment_data.py -n 10000 > inventory_data.json
python fake_websale_data_generator.py -n 10000 > websale_data.json
